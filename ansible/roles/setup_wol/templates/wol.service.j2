[Unit]
Description=Wake-on-LAN for {{ interface }}
Requires=network.target
After=network.target

[Service]
ExecStart=/sbin/ethtool -s {{ interface }} wol {{ value }}
ExecStop=/sbin/ethtool -s {{ interface }} wol {{ value }}

[Install]
WantedBy=multi-user.target