[Unit]
Description=Wake-on-LAN for {{ interface }}
Requires=network.target
After=network.target

[Service]
ExecStart=/sbin/ethtool -s {{ interface }} wol {{ wol_value }}
ExecStop=/sbin/ethtool -s {{ interface }} wol {{ wol_value }}

[Install]
WantedBy=multi-user.target
