[Unit]
Description=Wake-on-LAN for {{ sw_interface }}
Requires=network.target
After=network.target

[Service]
ExecStart=/sbin/ethtool -s {{ sw_interface }} wol {{ sw_value }}
ExecStop=/sbin/ethtool -s {{ sw_interface }} wol {{ sw_value }}

[Install]
WantedBy=multi-user.target
